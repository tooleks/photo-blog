server {
    include default;
    listen 8082;
    root /data/backend/public;

    location / {
        deny all;
        return 404;
    }

    location ~* _[0-9]+x[0-9]+\.(jpe?g|png|gif)$ {
        include max_cache;
    }
}
